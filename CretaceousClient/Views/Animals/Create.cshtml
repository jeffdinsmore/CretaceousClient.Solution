@* @{
  Layout = "_Layout";
} *@

@model CretaceousClient.Models.Animal;

<h4>Add a new Animal</h4>

@* <form action="/Animals/Index)" method="post">
  <label for="name">Animal's Name</label>
  <input id="name" name="name" type="text" value="@(Model.Name)">
  <label for="species">Animal's Species</label>
  <input id="species" name="species" type="text" value="@(Model.Species)">
  <label for="age">Animal's Age</label>
  <input id="age" name="age" type="number" value="@(Model.Age)">
  <label for="gender">Animal's Gender</label>
  <input id="gender" name="gender" type="text" value="@(Model.Gender)">
  <button type="submit">Go!</button>
</form> *@

@using (Html.BeginForm())
{
  @Html.Label("Animal Name:")
  <br>
  @Html.TextBoxFor(model => model.Name, new {@required="required"})
  <br><br>
  @Html.Label("Species:")
  <br>
  @Html.TextBoxFor(model => model.Species, new {@required="required"})
  <br><br>
  @Html.Label("Age:")
  <br>
  @Html.TextBoxFor(model => model.Age, new {@required="required"})
  <br><br>
  @Html.Label("Gender:")
  <br>
  @Html.TextBoxFor(model => model.Gender, new {@required="required"})
  <br><br>
  
  <input type="submit" value="Add New Animal" method="post" class="btn btn-success btn-sm" />
}
<br>
<p>@Html.ActionLink("Cancel", "Index", null, null, new {@class="btn btn-primary btn-sm"})</p>